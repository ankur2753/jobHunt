Look at this screenshot of a job application's final state. Please extract the following information and return it strictly as a valid JSON object. Do not include markdown code blocks. Include the keys: "company" (the hiring company), "position" (the job title), and "is_success" (boolean true if the screen clearly shows a successful submission confirmation like 'Application submitted', false otherwise). If is_success is false, include a "reason" key briefly explaining WHY the application failed or what screen it is stuck on in 1 short sentence.
